# YCC UX 완성도 보강 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 영천중앙교회 홈페이지에 로딩/에러/404 상태, JSON-LD 구조화 데이터, 이미지 트랜스포메이션 쿼터 제거를 추가해 완성도를 높인다.

**Architecture:** 세 갈래 모두 추가형(additive). (1) JSON-LD는 순수 빌더 함수(TDD) + `<JsonLd>` 직렬화 컴포넌트로 분리해 root layout·상세 페이지에 주입. (2) 로딩/에러/404는 App Router 규약 파일과 공용 스켈레톤 1개로 처리. (3) 이미지는 `next.config.ts` 한 줄.

**Tech Stack:** Next.js App Router, TypeScript, Tailwind v4(@theme 토큰), vitest, schema.org JSON-LD.

---

## File Structure

**생성:**
- `src/lib/seo/jsonld.ts` — 순수 빌더: `buildChurchJsonLd`, `buildSermonVideoJsonLd`, `buildBreadcrumbJsonLd`, `secondsToIsoDuration`, `serializeJsonLd`.
- `src/lib/seo/jsonld.test.ts` — 위 함수 단위 테스트(vitest).
- `src/components/seo/JsonLd.tsx` — `<script type="application/ld+json">` 렌더(XSS-safe 직렬화 사용).
- `src/components/ui/PageSkeleton.tsx` — 공용 스켈레톤(`grid`/`article` variant).
- `src/app/not-found.tsx`, `src/app/error.tsx`, `src/app/global-error.tsx`.
- `loading.tsx` × 8: `src/app/{sermons,news,bulletins,gallery}/loading.tsx` 및 각 `[id]/loading.tsx`.

**수정:**
- `src/app/layout.tsx` — Church JSON-LD 주입.
- `src/app/sermons/[id]/page.tsx` — VideoObject + Breadcrumb 주입.
- `src/app/news/[id]/page.tsx`, `src/app/bulletins/[id]/page.tsx` — Breadcrumb 주입.
- `next.config.ts` — `images.unoptimized = true`.

**검증 토큰(기존 코드 기준):** 헤딩 폰트 `font-serif`, 색상 `text-ink`/`text-ink-muted`/`text-faint`/`text-accent`/`bg-surface`/`border-line`/`bg-paper`/`text-paper`/`bg-ink`.

---

## Task 1: JSON-LD 순수 빌더 + 직렬화 (TDD)

**Files:**
- Create: `src/lib/seo/jsonld.ts`
- Test: `src/lib/seo/jsonld.test.ts`

- [ ] **Step 1: 실패하는 테스트 작성**

`src/lib/seo/jsonld.test.ts`:

```ts
import { describe, expect, it } from 'vitest'
import {
  buildBreadcrumbJsonLd,
  buildChurchJsonLd,
  buildSermonVideoJsonLd,
  secondsToIsoDuration,
  serializeJsonLd,
} from './jsonld'

describe('secondsToIsoDuration', () => {
  it('1초 미만/undefined는 undefined', () => {
    expect(secondsToIsoDuration(undefined)).toBeUndefined()
    expect(secondsToIsoDuration(0)).toBeUndefined()
  })
  it('초를 ISO 8601 기간으로 변환', () => {
    expect(secondsToIsoDuration(45)).toBe('PT45S')
    expect(secondsToIsoDuration(90)).toBe('PT1M30S')
    expect(secondsToIsoDuration(3661)).toBe('PT1H1M1S')
  })
})

describe('buildChurchJsonLd', () => {
  it('Church 스키마와 sameAs를 만든다', () => {
    const ld = buildChurchJsonLd()
    expect(ld['@type']).toBe('Church')
    expect(ld.name).toBe('영천중앙교회')
    expect(Array.isArray(ld.sameAs)).toBe(true)
    expect((ld.sameAs as string[]).some((u) => u.includes('youtube'))).toBe(true)
  })
})

describe('buildSermonVideoJsonLd', () => {
  it('base 필수 필드(name·uploadDate)는 항상 포함', () => {
    const ld = buildSermonVideoJsonLd({ name: '주일예배', uploadDate: '2026-06-21' })
    expect(ld.name).toBe('주일예배')
    expect(ld.uploadDate).toBe('2026-06-21')
  })
  it('youtubeId가 있으면 embedUrl/contentUrl 포함', () => {
    const ld = buildSermonVideoJsonLd({
      name: '주일예배',
      uploadDate: '2026-06-21',
      youtubeId: 'abc123',
      durationSeconds: 90,
    })
    expect(ld['@type']).toBe('VideoObject')
    expect(ld.embedUrl).toBe('https://www.youtube.com/embed/abc123')
    expect(ld.duration).toBe('PT1M30S')
  })
  it('durationSeconds 없으면 duration 키 생략', () => {
    const ld = buildSermonVideoJsonLd({ name: 'x', uploadDate: '2026-06-21' })
    expect('duration' in ld).toBe(false)
  })
})

describe('buildBreadcrumbJsonLd', () => {
  it('position 1..n과 절대 URL을 만든다', () => {
    const ld = buildBreadcrumbJsonLd([
      { name: '홈', path: '/' },
      { name: '설교', path: '/sermons' },
    ])
    const items = ld.itemListElement as Array<Record<string, unknown>>
    expect(items[0].position).toBe(1)
    expect(items[1].position).toBe(2)
    expect(String(items[1].item)).toMatch(/^https?:\/\/.+\/sermons$/)
  })
})

describe('serializeJsonLd', () => {
  it('< 를 \\u003c 로 이스케이프해 script 주입을 막는다', () => {
    const out = serializeJsonLd({ x: '</script>' })
    expect(out).not.toContain('</script>')
    expect(out).toContain('\\u003c')
  })
})
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `npm run test -- src/lib/seo/jsonld.test.ts`
Expected: FAIL — `Cannot find module './jsonld'`

- [ ] **Step 3: 구현 작성**

`src/lib/seo/jsonld.ts`:

```ts
import { churchInfo } from '@/lib/church'
import { absoluteUrl, getCanonicalSiteOrigin } from '@/lib/site-origin'

export const CHURCH_NAME = '영천중앙교회'

type JsonLdObject = Record<string, unknown>

export function buildChurchJsonLd(): JsonLdObject {
  return {
    '@context': 'https://schema.org',
    '@type': 'Church',
    name: CHURCH_NAME,
    url: getCanonicalSiteOrigin(),
    logo: absoluteUrl('/brand/pck-icon-512.png'),
    image: absoluteUrl('/brand/pck-og.png'),
    telephone: churchInfo.phone,
    address: {
      '@type': 'PostalAddress',
      streetAddress: churchInfo.address,
      addressCountry: 'KR',
    },
    sameAs: [churchInfo.youtube, churchInfo.blog],
  }
}

export function secondsToIsoDuration(seconds: number | undefined): string | undefined {
  if (!seconds || seconds < 1) return undefined
  const total = Math.floor(seconds)
  const h = Math.floor(total / 3600)
  const m = Math.floor((total % 3600) / 60)
  const s = total % 60
  return `PT${h ? `${h}H` : ''}${m ? `${m}M` : ''}${s ? `${s}S` : ''}`
}

export interface SermonVideoInput {
  name: string
  uploadDate: string
  description?: string
  thumbnailUrl?: string
  youtubeId?: string
  durationSeconds?: number
}

export function buildSermonVideoJsonLd(input: SermonVideoInput): JsonLdObject {
  const video: JsonLdObject = {
    '@context': 'https://schema.org',
    '@type': 'VideoObject',
    name: input.name,
    uploadDate: input.uploadDate,
  }
  if (input.description) video.description = input.description
  if (input.thumbnailUrl) video.thumbnailUrl = input.thumbnailUrl
  if (input.youtubeId) {
    video.embedUrl = `https://www.youtube.com/embed/${input.youtubeId}`
    video.contentUrl = `https://www.youtube.com/watch?v=${input.youtubeId}`
  }
  const duration = secondsToIsoDuration(input.durationSeconds)
  if (duration) video.duration = duration
  return video
}

export interface BreadcrumbItem {
  name: string
  path: string
}

export function buildBreadcrumbJsonLd(items: BreadcrumbItem[]): JsonLdObject {
  return {
    '@context': 'https://schema.org',
    '@type': 'BreadcrumbList',
    itemListElement: items.map((item, index) => ({
      '@type': 'ListItem',
      position: index + 1,
      name: item.name,
      item: absoluteUrl(item.path),
    })),
  }
}

export function serializeJsonLd(data: JsonLdObject | JsonLdObject[]): string {
  return JSON.stringify(data).replace(/</g, '\\u003c')
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `npm run test -- src/lib/seo/jsonld.test.ts`
Expected: PASS (5 describe 블록 전부 통과)

- [ ] **Step 5: 커밋**

```bash
git add src/lib/seo/jsonld.ts src/lib/seo/jsonld.test.ts
git commit -m "feat: JSON-LD 구조화데이터 빌더 추가"
```

---

## Task 2: JsonLd 컴포넌트 + 전역 Church 스키마

**Files:**
- Create: `src/components/seo/JsonLd.tsx`
- Modify: `src/app/layout.tsx`

- [ ] **Step 1: JsonLd 컴포넌트 작성**

`src/components/seo/JsonLd.tsx`:

```tsx
import { serializeJsonLd } from '@/lib/seo/jsonld'

type JsonLdData = Record<string, unknown> | Record<string, unknown>[]

export default function JsonLd({ data }: { data: JsonLdData }) {
  return (
    <script
      type="application/ld+json"
      dangerouslySetInnerHTML={{ __html: serializeJsonLd(data) }}
    />
  )
}
```

- [ ] **Step 2: root layout에 Church 스키마 주입**

`src/app/layout.tsx` — import 추가:

```tsx
import JsonLd from '@/components/seo/JsonLd'
import { buildChurchJsonLd } from '@/lib/seo/jsonld'
```

`<body>` 여는 태그 바로 다음(Skip 링크 위 또는 아래 무관, body 직속)에 추가:

```tsx
<JsonLd data={buildChurchJsonLd()} />
```

- [ ] **Step 3: 빌드로 검증**

Run: `npm run build`
Expected: 빌드 성공. (타입/import 오류 없음)

- [ ] **Step 4: 산출 확인**

Run: `npm run start` 후 별도 셸에서 `curl -s localhost:3000 | grep -o 'application/ld+json'`
Expected: `application/ld+json` 최소 1건 출력. 확인 후 start 종료.

- [ ] **Step 5: 커밋**

```bash
git add src/components/seo/JsonLd.tsx src/app/layout.tsx
git commit -m "feat: 전역 Church JSON-LD 주입"
```

---

## Task 3: 설교 상세 VideoObject + Breadcrumb

**Files:**
- Modify: `src/app/sermons/[id]/page.tsx`

기존 컴포넌트는 `getSermonById(id)` → `sermon`(필드: `youtubeId`, `sermonDate`, `thumbnailUrl`, `summary`, `durationSeconds`), 제목은 `sermonListTitle(sermon)`.

- [ ] **Step 1: import 추가**

`src/app/sermons/[id]/page.tsx` 상단 import에 추가:

```tsx
import JsonLd from '@/components/seo/JsonLd'
import { buildBreadcrumbJsonLd, buildSermonVideoJsonLd } from '@/lib/seo/jsonld'
```

- [ ] **Step 2: 상세 컴포넌트에 JSON-LD 주입**

`SermonDetailPage`의 `if (!sermon) notFound()` 다음, return JSX 안에서 `<Container>` 위(또는 `<div className="py-16">` 첫 자식)에 추가:

```tsx
const title = sermonListTitle(sermon)
```

return 내부 첫 요소로:

```tsx
<JsonLd
  data={[
    buildSermonVideoJsonLd({
      name: title,
      uploadDate: sermon.sermonDate,
      description: sermon.summary,
      thumbnailUrl: sermon.thumbnailUrl,
      youtubeId: sermon.youtubeId,
      durationSeconds: sermon.durationSeconds,
    }),
    buildBreadcrumbJsonLd([
      { name: '홈', path: '/' },
      { name: '말씀과 찬양', path: '/sermons' },
      { name: title, path: `/sermons/${sermon.id}` },
    ]),
  ]}
/>
```

(주의: `sermon.youtubeId`가 타입상 없으면 `sermon.youtubeVideoId`를 사용. 두 필드 모두 `Sermon` 타입에 존재하며 `youtubeId`가 정규화 값이다.)

- [ ] **Step 3: 빌드 검증**

Run: `npm run build`
Expected: 성공.

- [ ] **Step 4: 커밋**

```bash
git add src/app/sermons/[id]/page.tsx
git commit -m "feat: 설교 상세 VideoObject·Breadcrumb JSON-LD 주입"
```

---

## Task 4: 소식·주보 상세 Breadcrumb

**Files:**
- Modify: `src/app/news/[id]/page.tsx`
- Modify: `src/app/bulletins/[id]/page.tsx`

- [ ] **Step 1: 소식 상세에 주입**

`src/app/news/[id]/page.tsx` import 추가:

```tsx
import JsonLd from '@/components/seo/JsonLd'
import { buildBreadcrumbJsonLd } from '@/lib/seo/jsonld'
```

`getPostById` 결과 `post`(필드 `id`, `title`)를 쓰는 기본 export 컴포넌트의 return JSX 첫 요소로 추가:

```tsx
<JsonLd
  data={buildBreadcrumbJsonLd([
    { name: '홈', path: '/' },
    { name: '소식', path: '/news' },
    { name: post.title, path: `/news/${post.id}` },
  ])}
/>
```

- [ ] **Step 2: 주보 상세에 주입**

`src/app/bulletins/[id]/page.tsx` import 추가:

```tsx
import JsonLd from '@/components/seo/JsonLd'
import { buildBreadcrumbJsonLd } from '@/lib/seo/jsonld'
```

`if (!bulletin) notFound()` 다음, return JSX(`<div className="py-12">`) 첫 자식으로:

```tsx
<JsonLd
  data={buildBreadcrumbJsonLd([
    { name: '홈', path: '/' },
    { name: '주보', path: '/bulletins' },
    { name: `${bulletin.bulletinDate} 주보`, path: `/bulletins/${bulletin.id}` },
  ])}
/>
```

- [ ] **Step 3: 빌드 검증**

Run: `npm run build`
Expected: 성공.

- [ ] **Step 4: 커밋**

```bash
git add src/app/news/[id]/page.tsx src/app/bulletins/[id]/page.tsx
git commit -m "feat: 소식·주보 상세 Breadcrumb JSON-LD 주입"
```

---

## Task 5: 이미지 트랜스포메이션 쿼터 제거

**Files:**
- Modify: `next.config.ts`

- [ ] **Step 1: unoptimized 플래그 추가**

`next.config.ts`의 `images: {` 블록 안, `remotePatterns` 위에 한 줄 추가:

```ts
images: {
  unoptimized: true,
  remotePatterns: [
    // ...기존 그대로
  ],
},
```

- [ ] **Step 2: 빌드 검증**

Run: `npm run build`
Expected: 성공. 빌드 로그에 이미지 최적화 관련 경고 없음.

- [ ] **Step 3: 커밋**

```bash
git add next.config.ts
git commit -m "fix: 이미지 unoptimized 설정으로 Vercel 트랜스포메이션 쿼터 제거"
```

---

## Task 6: 공용 스켈레톤 + loading.tsx

> **결정(codex 리뷰 반영):** loading.tsx는 **목록 라우트 4곳만** 둔다. 상세 라우트(`[id]`)는 `generateStaticParams`로 사전생성(SSG)되어 로딩 이득이 적고, 무존재 id 접근 시 `notFound()`가 200 스트리밍(soft-404)으로 새는 위험이 있어 제외한다. 목록 라우트는 `notFound()`를 호출하지 않아 무관하다.

**Files:**
- Create: `src/components/ui/PageSkeleton.tsx`
- Create: `src/app/sermons/loading.tsx`
- Create: `src/app/news/loading.tsx`
- Create: `src/app/bulletins/loading.tsx`
- Create: `src/app/gallery/loading.tsx`

- [ ] **Step 1: PageSkeleton 작성**

`src/components/ui/PageSkeleton.tsx`:

```tsx
import Container from '@/components/layout/Container'

function Bar({ className = '' }: { className?: string }) {
  return <div className={`animate-pulse rounded-md bg-surface ${className}`} aria-hidden="true" />
}

export function PageSkeleton({ variant }: { variant: 'grid' | 'article' }) {
  return (
    <div className="py-16" role="status" aria-label="콘텐츠를 불러오는 중입니다">
      <Container className="max-w-5xl">
        <Bar className="mb-8 h-9 w-48" />
        {variant === 'grid' ? (
          <div className="grid grid-cols-1 gap-6 sm:grid-cols-2 lg:grid-cols-3">
            {Array.from({ length: 6 }).map((_, i) => (
              <div key={i} className="space-y-3">
                <Bar className="aspect-video w-full" />
                <Bar className="h-5 w-3/4" />
                <Bar className="h-4 w-1/2" />
              </div>
            ))}
          </div>
        ) : (
          <div className="space-y-4">
            <Bar className="h-7 w-2/3" />
            <Bar className="h-4 w-1/3" />
            <Bar className="mt-6 aspect-video w-full" />
            <Bar className="h-4 w-full" />
            <Bar className="h-4 w-full" />
            <Bar className="h-4 w-5/6" />
          </div>
        )}
      </Container>
    </div>
  )
}
```

- [ ] **Step 2: 목록 loading.tsx 4개 작성 (grid)**

아래 동일 내용을 `src/app/sermons/loading.tsx`, `src/app/news/loading.tsx`, `src/app/bulletins/loading.tsx`, `src/app/gallery/loading.tsx` 각각 생성:

```tsx
import { PageSkeleton } from '@/components/ui/PageSkeleton'

export default function Loading() {
  return <PageSkeleton variant="grid" />
}
```

(상세 `[id]/loading.tsx`는 만들지 않는다 — 위 결정 참고. `variant="article"`은 추후 필요 시 사용.)

- [ ] **Step 3: 빌드 검증**

Run: `npm run build`
Expected: 성공. 목록 4개 라우트에 loading 청크 생성.

- [ ] **Step 4: 커밋**

```bash
git add src/components/ui/PageSkeleton.tsx "src/app/sermons/loading.tsx" "src/app/news/loading.tsx" "src/app/bulletins/loading.tsx" "src/app/gallery/loading.tsx"
git commit -m "feat: 공개 목록 라우트 로딩 스켈레톤 추가"
```

---

## Task 7: 브랜드 404

**Files:**
- Create: `src/app/not-found.tsx`

- [ ] **Step 1: not-found.tsx 작성**

`src/app/not-found.tsx`:

```tsx
import Link from 'next/link'
import Container from '@/components/layout/Container'

export default function NotFound() {
  return (
    <div className="py-28 sm:py-36">
      <Container className="max-w-2xl text-center">
        <p className="text-sm font-bold tracking-widest text-accent">404</p>
        <h1 className="mt-4 font-serif text-3xl font-extrabold text-ink sm:text-4xl">
          페이지를 찾을 수 없습니다
        </h1>
        <p className="mt-4 text-ink-muted">
          요청하신 페이지가 이동되었거나 존재하지 않습니다.
        </p>
        <div className="mt-8 flex flex-wrap justify-center gap-3">
          <Link
            href="/"
            className="rounded-full bg-ink px-6 py-3 text-sm font-bold text-paper transition hover:opacity-90"
          >
            홈으로
          </Link>
          <Link
            href="/sermons"
            className="rounded-full border border-line px-6 py-3 text-sm font-bold text-ink transition hover:bg-surface"
          >
            설교 보기
          </Link>
        </div>
      </Container>
    </div>
  )
}
```

- [ ] **Step 2: 빌드 검증**

Run: `npm run build`
Expected: 성공.

- [ ] **Step 3: 커밋**

```bash
git add src/app/not-found.tsx
git commit -m "feat: 브랜드 404 페이지 추가"
```

---

## Task 8: 에러 바운더리

**Files:**
- Create: `src/app/error.tsx`
- Create: `src/app/global-error.tsx`

- [ ] **Step 1: error.tsx 작성 (클라이언트)**

`src/app/error.tsx`:

```tsx
'use client'

import { useEffect } from 'react'
import Link from 'next/link'
import Container from '@/components/layout/Container'

export default function Error({
  error,
  reset,
}: {
  error: Error & { digest?: string }
  reset: () => void
}) {
  useEffect(() => {
    console.error(error)
  }, [error])

  return (
    <div className="py-28 sm:py-36">
      <Container className="max-w-2xl text-center">
        <h1 className="font-serif text-3xl font-extrabold text-ink sm:text-4xl">
          문제가 발생했습니다
        </h1>
        <p className="mt-4 text-ink-muted">
          잠시 후 다시 시도해 주세요. 문제가 계속되면 교회로 문의해 주세요.
        </p>
        <div className="mt-8 flex flex-wrap justify-center gap-3">
          <button
            onClick={reset}
            className="rounded-full bg-ink px-6 py-3 text-sm font-bold text-paper transition hover:opacity-90"
          >
            다시 시도
          </button>
          <Link
            href="/"
            className="rounded-full border border-line px-6 py-3 text-sm font-bold text-ink transition hover:bg-surface"
          >
            홈으로
          </Link>
        </div>
      </Container>
    </div>
  )
}
```

- [ ] **Step 2: global-error.tsx 작성 (루트 폴백, 자체 html/body)**

`src/app/global-error.tsx`:

```tsx
'use client'

export default function GlobalError({
  reset,
}: {
  error: Error & { digest?: string }
  reset: () => void
}) {
  return (
    <html lang="ko">
      <body
        style={{
          fontFamily: 'sans-serif',
          display: 'flex',
          minHeight: '100vh',
          alignItems: 'center',
          justifyContent: 'center',
          padding: '2rem',
          textAlign: 'center',
        }}
      >
        <div>
          <h1 style={{ fontSize: '1.5rem', fontWeight: 700 }}>문제가 발생했습니다</h1>
          <p style={{ marginTop: '0.75rem', color: '#555' }}>페이지를 새로고침해 주세요.</p>
          <button
            onClick={reset}
            style={{
              marginTop: '1.5rem',
              padding: '0.75rem 1.5rem',
              borderRadius: 9999,
              background: '#111',
              color: '#fff',
              border: 'none',
              cursor: 'pointer',
            }}
          >
            다시 시도
          </button>
        </div>
      </body>
    </html>
  )
}
```

- [ ] **Step 3: 빌드 + 전체 테스트 검증**

Run: `npm run build && npm run test`
Expected: 빌드 성공 + 기존/신규 테스트 전부 통과.

- [ ] **Step 4: 커밋**

```bash
git add src/app/error.tsx src/app/global-error.tsx
git commit -m "feat: 에러 바운더리·global-error 폴백 추가"
```

---

## 최종 검증

- [ ] `npm run build` 성공
- [ ] `npm run test` 전부 통과
- [ ] `npm run start` 후 수동 확인:
  - `/sermons/없는id` → 브랜드 404 노출
  - **HTTP status 확인**: `curl -I http://localhost:3000/sermons/없는id` → `404` (200 soft-404 아님). 상세 loading.tsx 미생성으로 404 유지되는지 검증.
  - 홈 소스에 `application/ld+json`(Church) 포함
  - 설교 상세 소스에 `VideoObject` + `BreadcrumbList` 포함
  - 이미지가 `/_next/image` 경유 없이 원본 URL로 로드 (네트워크 탭)
- [ ] **SEO 유효성**: 빌드 산출 HTML의 JSON-LD를 Google Rich Results Test / Schema Markup Validator에 붙여 오류 없음 확인.
- [ ] **성능(unoptimized 회귀 점검)**: Lighthouse로 갤러리·홈 LCP/CLS 확인. 큰 png가 LCP를 끌면 해당 png만 webp 변환(선택적 후속).
- [ ] **환경변수**: 프로덕션 배포에 `NEXT_PUBLIC_SITE_URL`이 정식 도메인(`https://ycjc.kr`)으로 고정돼 JSON-LD/Breadcrumb 절대 URL이 preview 도메인으로 새지 않는지 확인.
